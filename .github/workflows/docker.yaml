name: Docker
on:
    push
jobs:
    idle:
        name: Docker build
        runs-on: ubuntu-latest
        steps:
            - 
                name: checkout
                uses: actions/checkout@v4
            -
                name: docker build
                run: docker build -t cicd24 .
